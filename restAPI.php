<?php
/*Creating a REST API in PHP involves setting up a system that
 allows different applications to communicate with each other over HTTP,
  typically using JSON or XML for data exchange. 
  it follows 4 http protocol
  soap(it support xml )
  xml-rpc(simple api)
  json-rpc(simple api)
  rest() 
  there are 3 types of api 
  1 open API (accessible everywhere )
  2 Partner API (paid api )
  3 internal API (work in localy) 
  
  rest api follow 4 http method 
  get(read data)
  put(update data)
  post(insert or create data)
  delete(delete the data )*/

?>